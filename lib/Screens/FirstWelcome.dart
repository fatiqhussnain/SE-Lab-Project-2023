import 'package:flutter/material.dart';

class FirstWelcome extends StatelessWidget {
  const FirstWelcome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('First Welcome'),
      ),
    );
  }
}
